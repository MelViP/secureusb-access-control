# secureusb-access-control
Python-based USB security solution using OpenSSL, Ubuntu, and Windows WMI. Verifies X.509 client certificates and automatically enforces read/write access on authorized devices while restricting unauthorized systems to read-only access.
